import * as React from "react"

/**
 * Hook to detect if the user is on a mobile device.
 * Modified to always return false to force desktop view.
 */
export function useIsMobile() {
  return false;
}
